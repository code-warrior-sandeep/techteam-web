<% layout("layouts/boilerplate.ejs") %>

<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-lg-10">

      <!-- Fun Student Dashboard Card -->
      <div class="card shadow-lg border-0 rounded-4 p-4 bg-light">
        <div class="row align-items-center">

          <!-- Profile Image + Username -->
          <div class="col-md-4 text-center mb-3 mb-md-0">
            <img 
               src="<%= studentData?.image?.url || '/images/dummy.png' %>" 
              alt="Profile Photo" 
              class="img-fluid rounded-circle shadow-sm border border-3 border-primary" 
              style="width: 150px; height: 150px; object-fit: cover;"
            >
            <h5 class="mt-3 fw-bold text-primary">
              👤 <%= studentData.username %>
            </h5>
            <span class="badge bg-success mt-1">🌟 Active </span>
          </div>

          <!-- Profile Info -->
          <div class="col-md-8">
            <h3 class="fw-bold mb-3 text-secondary">
              📚 Student Profile Overview
            </h3>
            <div class="row g-3">
              <div class="col-sm-6">
                <p>👨‍🎓 <strong>Name:</strong> <%= studentData.name %></p>
              </div>
              <div class="col-sm-6">
                <p>📧 <strong>Email:</strong> <%= studentData.email %></p>
              </div>
              <div class="col-sm-6">
                <p>🎫 <strong>Roll Number:</strong> <%= studentData.rollNumber %></p>
              </div>
              <div class="col-sm-6">
                <p>📅 <strong>Semester:</strong> <%= studentData.semester %></p>
              </div>
              <div class="col-sm-6">
                <p>🛠️ <strong>Role:</strong> <%= studentData.roles %></p>
              </div>
              <div class="col-sm-6">
                <p>📆 <strong>Joined:</strong> <%= studentData.createdAt.toDateString() %></p>
              </div>
            </div>

            <!-- Bonus Message -->
            <div class="alert alert-warning mt-4 d-flex align-items-center" role="alert">
              <i class="fas fa-lightbulb me-2 text-warning"></i>
              Keep shining, <strong class="ms-1"><%= studentData.name.split(" ")[0] %></strong>! Welcome  ✨
            </div>
          </div>

        </div>
      </div>

    </div>
  </div>
</div>
